debug: false
coin:
  # Custom item ID (default: cotr:coin)
  # Use this format to register a custom item via data pack
  # Format: "namespace:item_name" (e.g., "cotr:coin")
  # If you use a custom item, the plugin will automatically generate and install
  # a data pack to register it. This allows /give commands to work.
  item: "cotr:coin"
  
  # Fallback material if custom item not available
  # Only used if custom item creation fails or if item is a vanilla material
  # This provides a fallback appearance for players without the resource pack
  fallback-item: "minecraft:gold_nugget"
  
  # Item model for resource pack (namespace:key format)
  # This references assets/cotr/items/coin.json in the resource pack
  # Format: "namespace:key" (e.g., "cotr:coin")
  model: "cotr:coin"
  
  # Display name for the coin
  display-name: "Coin of the Realm"
  
  # Lore lines for the coin
  lore:
    - "A valuable currency"
    - "used throughout the realm."
  
  # Max stack size (1-64, default: 64)
  max-stack-size: 64
  
  # Rarity: common, uncommon, rare, epic (default: uncommon)
  rarity: "uncommon"

resource-pack:
  # URL to the resource pack ZIP file
  # This will be automatically applied to players when they join
  # Leave empty to disable automatic resource pack application
  url: "https://github.com/radawson/cotr/raw/refs/heads/main/src/main/resources/cotr-resources.zip"
  
  # SHA-1 hash of the resource pack (optional, but recommended for security)
  # Leave empty to skip hash verification
  # You can get the hash using: sha1sum cotr-resources.zip
  hash: "3a2ba2a79151c1e5fee471c4e31450a8bd270cdb"
  
  # Whether to prompt players to accept the resource pack
  # If false, resource pack will be applied automatically (may require server.properties setting)
  prompt: true

banking:
  enabled: true
  default-account-pattern: "{player-uuid}-main"
  membership-storage: "yaml"
